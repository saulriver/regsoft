
</br>

<div class="alert">
  <p id="notice" class="alert alert-success">
    <%= notice %>
      <strong>Asignación Area!</strong> Actualizado. 
  </p>
</div>

<div class="modal-content text-center tabcontent">
<div class="table-responsive">
  <table class="table">

    <thead>
      <div class="row">
        <tr class="success btn-info active justify-content-center">
          <th colspan="2">Id</th>
          <th>Usuario</th>
          <th>Role</th>
          <th>Area</th>
          <th>Estado</th>
        </tr>
      </div>
    </thead>

    <tbody>
      <div class="row">
      <tr class="text-center">
        <td colspan="2"><%= @userarea.id%></td>
        <td><%= @userarea.user.name %></td>
        <td><%= @userarea.user.role.name %></td>
        <td><%= @userarea.area.name %></td>
        <td><%= @userarea.state %></td>
        </tr>
      </div>
    </tbody>
  </table>
</div>
</div>

<div class="actions">
</br>
<% comment= link_to 'Editar', edit_userarea_path(@userarea) %>
<%= link_to 'Regresar', userareas_path, class: "btn btn-info" %>
</div>